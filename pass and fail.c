// find pass and fail
#include<stdio.h>
//header file
// main calling function
main()
{
    int number;
    printf("inter the number:");
    scanf("%d",&number);
    if (number >= 50)
        printf("this is the pass");
    else
        printf("this is the fail");
}
